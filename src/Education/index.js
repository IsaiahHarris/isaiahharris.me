import React, { Component } from 'react';

const education = props => (
  <div className="education-container">
    <div className="header">education works</div>
  </div>
);
export default education;
